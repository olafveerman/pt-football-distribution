# portugal-football-map

## Workflow

- Always make changes on a feature branch, never directly on `main`.
- Name branches descriptively (e.g. `feature/historical-data`, `fix/popup-layout`).
- When a task is complete, open a PR from the feature branch into `main`.
- Do not push to `main` directly.
